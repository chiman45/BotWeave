# Core
flask>=3.0.0
flask-cors>=4.0.0
gunicorn>=21.0.0
python-dotenv>=1.0.0

# Twilio
twilio>=8.0.0

# MongoDB
pymongo>=4.0.0

# HTTP
requests>=2.31.0

# RAG — vector store + keyword search
chromadb>=0.5.0
rank-bm25>=0.2.2

# Language detection (optional — graceful fallback to English if missing)
langdetect>=1.0.9
