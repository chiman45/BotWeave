twilio>=8.0.0
python-dotenv>=1.0.0
pymongo>=4.0.0
flask>=3.0.0
flask-cors>=4.0.0
gunicorn>=21.0.0
pyngrok>=7.0.0
requests>=2.31.0

# ── AI / LLM (optional — only needed for AI bots)
# Only chromadb is needed; Ollama is called via its REST API (uses 'requests' above)
chromadb>=0.5.0

# ── Web scraper (optional — only needed to run iiitnr_scraper.py)
beautifulsoup4>=4.12.0
lxml>=5.0.0
